import 'package:flutter/material.dart';

class CreateLiftPage extends StatelessWidget {
  const CreateLiftPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body:Text("olaaaa"),
    );
  }
}